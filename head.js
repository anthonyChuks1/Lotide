/**
 * Returns the first item in an array or as a single value. 
 * @param {Array} arr_ - The input array.
 * @returns {*} The first item in the array.
 */
const head = function(arr_) {
  let getVal = arr_[0];
  return getVal;
};

module.exports = head;